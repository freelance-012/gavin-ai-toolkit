# 项目规格文件模板

> 此模板供 Phase 2 生成 `{project}/.specs/project-spec.md` 时套用

---

# 项目规格说明

> **项目**: {项目名称}
> **路径**: {project_path}
> **生成日期**: {YYYY-MM-DD HH:MM}
> **最后更新**: {YYYY-MM-DD HH:MM}
> **生成方式**: slam-project-profiler

---

## 1. 项目基本信息

| 属性 | 值 | 来源 |
|------|-----|------|
| 项目名称 | {name} | {README / 用户指定} |
| 简述 | {一句话描述项目功能} | {README / 用户指定} |
| 框架 | ROS1 / ROS2 / 独立运行 | {package.xml / CMakeLists / 用户指定} |
| 主要语言 | C++ / Python / 混合 | {文件统计} |
| 构建系统 | CMake / Catkin / Colcon / Make | {构建文件} |
| 代码版本 | `{git commit hash}` | {git} |

---

## 2. 数据集

### 2.1 数据集概览

| 属性 | 值 | 来源 |
|------|-----|------|
| 数据集名称/来源 | {name} | {README / 用户指定} |
| 数据目录 | {绝对路径} | {用户指定} |
| 传感器配置 | 单目+IMU / 双目+IMU / LiDAR+IMU / ... | {配置文件 / 用户指定} |
| 时间戳单位 | 秒 / 毫秒 / 纳秒 | {代码 / 用户指定} |
| 坐标系 | FLU / ENU / NED / 其他 | {文档 / 用户指定} |

### 2.2 数据文件结构

```
{数据目录}/
├── {seq_00}/
│   ├── cam0/                    # {图像格式，如 PNG}
│   ├── cam1/                    # {如果有双目}
│   ├── imu0.csv                 # {IMU 数据格式}
│   └── ...
├── {seq_01}/
│   └── ...
└── ...
```

### 2.3 各传感器数据格式

#### 图像

| 属性 | 值 |
|------|-----|
| 格式 | {PNG / JPG / RAW} |
| 目录模式 | `{数据目录}/{seq}/cam0/{filename}` |
| 文件命名规则 | {时间戳.png / 序号.png / 其他} |
| 分辨率 | {宽x高，如果已知} |

#### IMU（如果有）

| 属性 | 值 |
|------|-----|
| 格式 | {CSV / 二进制 / ROS bag} |
| 文件路径 | `{数据目录}/{seq}/imu0.csv` |
| 列含义 | {timestamp, gx, gy, gz, ax, ay, az} |
| 时间戳单位 | {秒 / 纳秒} |
| 角速度单位 | {rad/s} |
| 加速度单位 | {m/s²} |
| 采样频率 | {Hz} |

#### LiDAR（如果有）

| 属性 | 值 |
|------|-----|
| 格式 | {pcap / bag / 自定义二进制} |
| 文件路径 | `{路径}` |
| 点云格式 | {XYZ / XYZI / XYZIR} |
| 线数 | {16 / 32 / 64 / 128} |

---

## 3. 真值

| 属性 | 值 | 来源 |
|------|-----|------|
| 真值文件 | {绝对路径} | {用户指定} |
| 格式 | TUM / EuRoC / KITTI / 自定义 | {代码 / 用户指定} |
| 列含义 | {逐列说明} | {代码 / 用户指定} |
| 时间戳单位 | {秒 / 纳秒} | {代码 / 用户指定} |
| 坐标系 | {FLU / ENU / NED} | {文档 / 用户指定} |
| 位置单位 | {米} | {用户指定} |
| 旋转表示 | {四元数 xyzw / wxyz / 旋转矩阵 / 欧拉角} | {代码 / 用户指定} |
| 与图像的时间对齐方式 | {最近邻 / 线性插值 / 其他} | {代码 / 用户指定} |

**真值文件示例**（前 3 行）:

```
{前3行内容}
```

---

## 4. 系统输出

### 4.1 轨迹输出

| 属性 | 值 | 来源 |
|------|-----|------|
| 轨迹文件 | {绝对路径或路径模式} | {代码 / 用户指定} |
| 格式 | TUM / EuRoC / KITTI / 自定义 | {代码 / 用户指定} |
| 列含义 | {逐列说明} | {代码 / 用户指定} |
| 时间戳单位 | {秒 / 纳秒} | {代码 / 用户指定} |
| 坐标系 | {与真值相同 / 不同} | {代码 / 用户指定} |

### 4.2 输出目录

| 属性 | 值 | 来源 |
|------|-----|------|
| 默认输出目录 | {路径模式} | {代码 / 配置} |
| 指定方式 | {命令行参数 / 配置文件 / 环境变量} | {代码} |

### 4.3 其他输出文件

| 文件 | 内容 | 路径 |
|------|------|------|
| {文件名} | {内容描述} | {路径} |
| ... | ... | ... |

---

## 5. 日志

### 5.1 日志概览

| 属性 | 值 | 来源 |
|------|-----|------|
| 日志框架 | ROS / glog / spdlog / cout / 自定义 | {代码} |
| 输出方式 | 终端 / 文件 / 两者 | {代码 / 配置} |
| 日志目录 | {绝对路径，如果输出到文件} | {配置 / 用户指定} |
| 日志级别控制 | {配置方式} | {代码 / 配置} |

### 5.2 日志格式

每行日志的结构:

```
{格式模板，如: [时间戳] [级别] [模块名] [文件:行号] 内容}
```

各字段说明:

| 字段 | 格式 | 示例 |
|------|------|------|
| 时间戳 | {格式} | {示例值} |
| 级别 | {格式} | {INFO / WARN / ERROR} |
| 模块名 | {格式} | {Frontend / Backend / ...} |
| 文件:行号 | {格式} | {file.cpp:123} |

### 5.3 模块标识

日志中出现的所有模块标识及其对应代码位置:

| 模块标识 | 对应代码模块 | 主要文件 |
|---------|------------|---------|
| `{标识}` | {模块功能} | `{file}:{line}` |
| ... | ... | ... |

### 5.4 关键日志条目

高频或关键的日志输出及其含义:

| # | 日志模式 | 含义 | 代码位置 |
|---|---------|------|---------|
| 1 | `{日志正则或前缀}` | {含义} | `{file}:{line}` |
| 2 | ... | ... | ... |

---

## 6. 评估

### 6.1 评估脚本

| 属性 | 值 | 来源 |
|------|-----|------|
| 脚本路径 | {绝对路径} | {用户指定} |
| 脚本类型 | Python / Shell / C++ / 其他 | {文件} |
| 运行命令 | `{完整命令}` | {用户指定} |
| 所需参数 | `{参数说明}` | {脚本 --help / 用户指定} |

### 6.2 评估输出格式

评估脚本的输出格式:

```
{输出示例或格式描述}
```

### 6.3 关注指标

| 指标 | 全称 | 含义 | 单位 | 合格阈值 |
|------|------|------|------|---------|
| {缩写} | {全称} | {含义} | {单位} | {阈值，如果已知} |
| ... | ... | ... | ... | ... |

### 6.4 评估流程

典型的评估流程:

```bash
# Step 1: 运行 SLAM 系统
{运行命令}

# Step 2: 运行评估脚本
{评估命令}

# Step 3: 查看结果
{结果查看方式}
```

---

## 7. 验证状态

> 以下记录 Phase 3 验证结果

| # | 验证项 | 状态 | 备注 |
|---|--------|------|------|
| 1 | 数据目录存在 | ✅ / ❌ / ⚠️ 待验证 | {备注} |
| 2 | 真值文件可读且格式匹配 | ✅ / ❌ / ⚠️ 待验证 | {备注} |
| 3 | 评估脚本可执行 | ✅ / ❌ / ⚠️ 待验证 | {备注} |
| 4 | 日志格式匹配 | ✅ / ❌ / ⚠️ 待验证 | {备注} |
| 5 | 轨迹输出格式匹配 | ✅ / ❌ / ⚠️ 待验证 | {备注} |

---

## 8. 更新记录

| 日期 | 变更内容 | 原因 |
|------|---------|------|
| {生成日期} | 初始生成 | slam-project-profiler 首次运行 |
| ... | ... | ... |

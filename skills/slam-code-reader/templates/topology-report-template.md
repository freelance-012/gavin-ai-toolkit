# 拓扑分析报告模板

> 此模板供 Phase 1 生成 `01_拓扑结构分析.md` 时套用

---

# 01_拓扑结构分析

> **仓库**: {仓库名}
> **生成日期**: {YYYY-MM-DD HH:MM}
> **分析者**: Gavin + AI

---

## 1. 项目概览

| 属性 | 值 |
|------|-----|
| 项目名称 | {仓库名} |
| 主要编程语言 | C++ / Python / CUDA (按实际) |
| 构建系统 | CMake / Make / Catkin (按实际) |
| 框架/平台 | ROS / 独立运行 / 嵌入式 (按实际) |
| 总源文件数 | {N} 个 (排除 third_party/, test/) |
| 总代码行数 | ~{N} 行 |

### 外部依赖

| 依赖库 | 版本要求 | 用途 |
|--------|---------|------|
| OpenCV | >= X.X | 图像处理 |
| Eigen | >= X.X | 矩阵运算 |
| Ceres Solver | >= X.X | 非线性优化 |
| ... | ... | ... |

---

## 2. 目录结构树

```
{仓庛名}/
├── {core_dir}/              # {推断的职责}
│   ├── {subdir}/            #   {职责}
│   │   ├── *.h
│   │   └── *.cpp
│   └── ...
├── {another_dir}/           # {职责}
├── third_party/             # (已折叠)
├── test/                    # (已折叠)
├── config/                  # 配置文件
├── CMakeLists.txt
└── README.md
```

> 注: `third_party/`, `test/`, `.git/`, `build/` 等非核心目录已折叠。

---

## 3. 模块职责映射表

| 目录/模块 | 推断职责 | 关键文件 | 置信度 | 备注 |
|-----------|---------|---------|--------|------|
| `{dir_name}` | {一句话描述} | `{file1.h}`, `{file2.cpp}` | ★★★★ 高 / ★★★ 中 / ★★ 低 | {补充说明} |
| ... | ... | ... | ... | ... |

置信度说明:
- ★★★★ 高: 目录名、代码内容、注释三者一致确认
- ★★★ 中: 目录名和内容基本吻合，但缺少明确注释
- ★★ 低: 仅根据命名推测，待后续 Phase 验证

---

## 4. 构建系统分析

### 4.1 编译目标

| 目标名 | 类型 | 源文件 | 说明 |
|--------|------|--------|------|
| `{target_name}` | 可执行文件 / 库 | `{src_files}` | {入口或用途} |

### 4.2 子模块划分

```
CMakeLists.txt (根)
├── add_subdirectory({module_a})    → {职责}
├── add_subdirectory({module_b})    → {职责}
└── add_subdirectory({module_c})    → {职责}
```

### 4.3 ROS 工程额外信息（如果是 ROS 项目）

- **package name**: `{pkg_name}`
- **依赖的 ROS 包**: `{list}`
- **Launch 文件**:
  - `{launch_file.launch}`: 启动 `{node_names}`

---

## 5. 入口点

| 入口类型 | 文件位置 | 行号 | 函数签名 | 说明 |
|---------|---------|------|---------|------|
| main 函数 | `{path}` | {N} | `int main(...)` | 主入口 |
| 回调入口 | `{path}` | {N} | `void callback(...)` | 数据回调 |
| 节点初始化 | `{path}` | {N} | `ros::init(...)` | ROS 初始化 |

### 启动流程简述

```
main()
  ├── 1. 读取配置参数
  ├── 2. 初始化各模块
  ├── 3. 注册回调 / 订阅话题
  ├── 4. 创建线程 / 启动 spin
  └── 5. 进入主循环
       ├── 线程A: {职责}
       ├── 线程B: {职责}
       └── 主线程: {职责}
```

---

## 6. 线程 / 节点模型

| 线程/节点 ID | 创建位置 | 职责 | 运行方式 |
|-------------|---------|------|---------|
| Thread / Node A | `{file}:{line}` | {一句话} | 循环 / 事件驱动 |
| Thread / Node B | `{file}:{line}` | {一句话} | 循环 / 事件驱动 |

### 线程间通信

| 数据 | 发送方 | 接收方 | 通信机制 | 同步方式 |
|------|--------|--------|---------|---------|
| `{data_name}` | {module} | {module} | queue / shared_var / topic | mutex / atomic / 无 |

---

## 7. 待确认项

以下内容在本次扫描中无法确定，需要后续 Phase 验证：

| # | 不确定项 | 所在位置 | 原因 | 建议验证方式 |
|---|---------|---------|------|-------------|
| 1 | `{what}` | `{where}` | `{why}` | {how to verify} |
| 2 | ... | ... | ... | ... |

# 模块精读文档模板（7-Section）

> 此模板供 Phase 4 生成 `04/{module-name}-deep-dive.md` 时套用
> 这是整个七步法中最核心的文档格式

---

# {模块名} 精读报告

> **仓库**: {仓库名}
> **模块路径**: `{完整文件路径}`
> **对应论文**: {论文名} ({作者}, {会议/期刊} {年份})
> **生成日期**: {YYYY-MM-DD HH:MM}
> **分析者**: Gavin + AI

---

## Section 1: 模块定位与职责

### 1.1 在数据流中的位置

```
{数据流简图，展示该模块在 pipeline 中的位置}

例: IMU 原始数据 → [本模块: 预积分] → 后端优化
```

| 属性 | 内容 |
|------|------|
| 数据流位置 | 第 {N} 环节（{上游} → **{本模块}** → {下游}） |
| 输入 | {输入数据类型及含义} |
| 输出 | {输出数据类型及含义} |
| 调用方 | `{调用函数}` @ `{file}:{line}` |
| 被调用方 | `{被调用的内部函数/子模块}` |

### 1.2 一句话概括

{用 1-2 句话说明这个模块解决什么问题、用什么方法}

---

## Section 2: 核心数据结构

### 2.1 主类定义

```cpp
// 文件: {file}:{line_start}-{line_end}
class {ClassName} {
public:
    // === 核心状态 ===
    {Type} {member_name_;   // 含义}
    {Type} {member_name_;   // 含义}

    // === 雅可比 / 协方差 (如果有) ===
    ...

    // === 缓存数据 ===
    ...

private:
    // === 配置参数 ===
    ...
};
```

### 2.2 变量 ↔ 论文符号映射表

| 代码变量 | 论文符号 | 物理含义 | 类型 | 定义位置 |
|---------|---------|---------|------|---------|
| `{var_}` | {symbol} | {meaning} | `{Type}` | `.h:{line}` |
| ... | ... | ... | ... | ... |

> 如果没有对应论文，此表改为"变量含义说明表"，去掉"论文符号"列。

---

## Section 3: 理论推导 → 代码逐行对照 ⭐

> 本 Section 是精读文档的核心价值所在。

### 3.1 {公式/算法名称 A}（论文 Eq.{N}）

#### 论文原文

$$
{LaTeX 公式}
$$

**来源**: {论文名} Section {N}, Equation {N}

#### 推导说明

- **物理含义**: {每项代表什么}
- **假设条件**: {离散化方式、噪声模型、线性化点选择等}
- **适用范围**: {什么情况下成立}

#### 对应代码

```cpp
// 文件: {file}:{line_start}-{line_end}
// 整体功能: {这段代码在做什么}

// 第 {N} 行: {这行代码的作用}
{code_line};  // ↑ 对应公式的哪一项

// 第 {N} 行: {这行代码的作用}
{code_line};  // ↑ 对应公式的哪一项

// ...
```

#### 对照注释

| 公式项 | 对应代码表达式 | 代码行号 | 备注 |
|--------|--------------|---------|------|
| {term_A} | `{code_expr}` | :{N} | {说明} |
| {term_B} | `{code_expr}` | :{N} | {说明} |

#### 差异记录

| # | 论文描述 | 代码实现 | 偏差原因 | 潜在影响 |
|---|---------|---------|---------|---------|
| 1 | {论文说法} | {代码做法} | {为什么不同} | {什么场景会有问题} |

---

### 3.2 {公式/算法名称 B}（论文 Eq.{N}）

（重复上述结构：论文原文 → 推导说明 → 对应代码 → 对照注释 → 差异记录）

---

### 3.3 {更多公式...}

（按需扩展）

---

## Section 4: 函数调用链

### 4.1 入口函数

```
{EntryFunction}() @ {file}:{line}
  └→ 功能: {一句话描述}
     参数: ({param}: {type}) → {含义}
     返回: ({type}) → {含义}
```

### 4.2 完整调用树

```
{入口函数} @ {file}:{line}
  ├── {InnerFunctionA}() @ {file}:{line}
  │   ├── {LowLevelX}() @ {file}:{line}
  │   │   └── 操作: {数学运算描述}
  │   └── {LowLevelY}() @ {file}:{line}
  │       └── 操作: {数学运算描述}
  ├── {InnerFunctionB}() @ {file}:{line}
  │   └── ...
  └── 返回: {返回值含义}
```

### 4.3 关键函数速查

| 函数名 | 文件:行号 | 功能 | 输入 | 输出 | 复杂度 |
|--------|-----------|------|------|------|--------|
| `{fn}` | `{f}:{l}` | {一句话} | {in} | {out} | O({n}) |

---

## Section 5: 关键参数清单

### 5.1 本模块涉及的参数

| 参数名 | 默认值 | 定义位置 | 所属类别 | 含义 | 推荐范围 | 敏感度 |
|--------|--------|---------|---------|------|---------|--------|
| `{param}` | `{val}` | `{f}:{l}` | {精度/鲁棒性/性能/硬件} | {meaning} | [{min}, {max}] | ★★★ 高 |

### 5.2 参数影响分析

> 用文字段落描述关键参数之间的相互影响和调参建议。

- **{参数组 A}**: 增大 → {后果1}; 减小 → {后果2}
- **{参数组 B}**: 与 {参数A} 的耦合关系: {描述}
- **常见误区**: {用户容易犯的错}

---

## Section 6: 问题与改进方向

| # | 问题 | 严重度 | 触发条件 | 当前表现 | 改进建议 |
|---|------|-------|---------|---------|---------|
| 1 | {问题描述} | ★★★ 高 / ★★ 中 / ★ 低 | {何时出现} | {具体现象} | {怎么改} |
| 2 | ... | ... | ... | ... | ... |

严重度:
- ★★★ 高: 影响精度或稳定性，生产环境必须解决
- ★★ 中: 特定场景下有问题，大多数情况可接受
- ★ 低: 理论上的不完美，实际影响很小

---

## Section 7: 同类实现对比

| 特征维度 | 本项目 | {框架A} | {框架B} | {框架C} |
|---------|--------|---------|---------|---------|
| 核心方法 | {本项目做法} | {A的做法} | {B的做法} | {C的做法} |
| 数据组织 | {...} | {...} | {...} | {...} |
| 计算效率 | O({n}) | O({n}) | O({n}) | O({n}) |
| 精度特点 | {...} | {...} | {...} | {...} |
| 适用场景 | {...} | {...} | {...} | {...} |

### 设计取舍分析

> 为什么本项目选择了当前方案？与其他方案的 trade-off 是什么？

- 选择 {方案} 是因为: {理由}
- 放弃 {替代方案} 是因为: {理由}
- 在 {场景} 下当前方案可能不足: {说明}
